[build]
  command = "npm install"
  publish = "."

[build.environment]
  NODE_VERSION = "18.0.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  framework = "#custom"
  targetPort = 3000
  command = "npm run dev"
  port = 8888
  publish = "."
